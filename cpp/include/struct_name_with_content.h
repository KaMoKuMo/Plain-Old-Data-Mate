#pragma once

#include <string>

/**
 * Rough cut of a struct string
 **/
struct StructNameWithContent {
    std::string name; //!< the name of the struct
    std::string content; //!< the content within the scope of the struct definition
};
